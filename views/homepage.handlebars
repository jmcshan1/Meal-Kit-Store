{{#each styles as |style| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
  </div>
  <div class="col-md-6">
    <h3><a href="mailto:">{{style.name}}</a></h3>
  </div>
</div>
{{/each}}
